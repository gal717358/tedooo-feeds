import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  root: {
    height: '58px',
    alignItems: 'center',
    background: 'white',
  },
  navBarContainer: {
    display: 'flex',
    flexDirection: 'row',
    margin: '0 160px',
    alignItems: 'center',
    height: '100%',
  },
});

export default useStyles;
